{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Core Docs",
            "group": "build",
            "command": "cargo",
            "args": [
                "doc",
            ],
            "options": {
                "cwd": "${workspaceRoot}"
            },
            "problemMatcher": [
                "$rustc"
            ]
        }
    ]
}
